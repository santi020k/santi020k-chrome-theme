{
  "name": "santi020k-chrome-theme-website",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":     "vite --host 127.0.0.1 --port 4175",
    "build":   "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4175"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}

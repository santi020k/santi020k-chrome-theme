{
  "name": "santi020k-chrome-theme",
  "version": "1.1.0",
  "description": "Chrome browser theme matching the Santi020k palette.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev":          "vite website --host 127.0.0.1 --port 4175",
    "build":        "vite build website",
    "preview":      "vite preview website --host 127.0.0.1 --port 4175",
    "validate":     "npm run package:dry",
    "sync:themes": "node scripts/sync-from-vscode-theme.mjs --variant=dark --write && node scripts/sync-from-vscode-theme.mjs --variant=light --write",
    "sync:assets": "node scripts/generate-ntp-image.mjs && node scripts/generate-ntp-image-light.mjs",
    "sync": "npm run sync:themes && npm run sync:assets",
    "package": "node scripts/package-extension.mjs",
    "package:dry":  "node scripts/package-extension.mjs --dry-run"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "vite": "^8.0.10"
  }
}
